\documentclass{report}
\usepackage[minted]{tcolorbox}
\setlength{\parindent}{0pt}
\setlength{\parskip}{1ex}

\tcbset{my-box-style/.style={
    colback=white,
    sharp corners,
}}

\tcbset{my-box-substyle-a/.style={
    my-box-style,
    colback=green!10,
}}
\makeatletter
\tcbset{my-box-substyle-b/.style={
    my-box-style,
    colback=blue!10,
    grow to left by=\kvtcb@left@rule+\kvtcb@boxsep+\kvtcb@leftupper,
    grow to right by=\kvtcb@right@rule+\kvtcb@boxsep+\kvtcb@rightupper
    }}
\makeatother

% My failed attempt:
% \tcbset{every box on layer 2/.style={
%     left=0pt,
%     right=0pt,
% }}
% End of my attempt.

\begin{document}
I want the inner boxes from here:

\begin{tcolorbox}[my-box-substyle-a]
    Outer box.
    \begin{tcolorbox}[my-box-substyle-b]
        Inner box.
    \end{tcolorbox}
\end{tcolorbox}
\ExplSyntaxOn
    \tl_new:N \g__Prassble_Before_tl
    \tcbset{
        Before/.code = {
            \tl_put_right:Nn \g__Prassble_Before_tl { #1~ }
            \pgfkeysalso{before = \g__Prassble_Before_tl}
        }
    }
\ExplSyntaxOff
\begin{tcolorbox}[Before={hello},Before={there}]
    tcolorbox
\end{tcolorbox}

\NewDocumentCommand{\foo}{ m }{\fbox{#1}}
\ExplSyntaxOn
    \AddToHook{cmd/\cs_to_str:N \foo/before}{Before\begingroup\par\centering}
    \AddToHook{cmd/\cs_to_str:N \foo/after}{\par\endgroup After}
\ExplSyntaxOff
\foo{Here}

\NewTCBInputListing{\inputcode}{ m }{
    ,listing engine=minted
    ,minted language=text
    ,listing file={#1}
    ,listing only
}
\ExplSyntaxOn
    \AddToHook{cmd/\cs_to_str:N \inputcode/before}{Before}
\ExplSyntaxOff

\inputcode{sample_code.tex}
\end{document}